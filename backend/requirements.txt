# fastapi: 0.136.1 부터 starlette 1.0 bump (PR #15397). 0.136.3 = header
# convert_underscores=True 새 default. 우리 코드 Header() 사용 0건 → 영향 0.
# 2026-05-31 lock: 0.135.3 → 0.136.3.
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
# sqlalchemy: 2.0.50 patch (2026-05-24) — ORM polymorphic·joinedload·JSONB 개선.
# 우리 신식 Mapped[] + mapped_column() 패턴 호환. 2026-05-31 lock: 2.0.49 → 2.0.50.
sqlalchemy[asyncio]>=2.0,<3
psycopg2-binary>=2.9,<3
curl_cffi>=0.5,<1
apscheduler>=3.10,<4
pandas>=2.0,<4
xlsxwriter>=3.1,<4
python-dotenv>=1.0,<2
# python-jose: 유지보수 정체 -> PyJWT 전환 (HS256 대칭키만, 비대칭키 0건 -> [crypto] extra 불필요).
# 2026-05-31 lock: python-jose>=3.3,<4 -> PyJWT>=2.0,<3.
PyJWT>=2.0,<3
python-multipart>=0.0.9,<1
redis>=5.0,<6
requests>=2.31,<3
# starlette: fastapi 의 transitive 의존. PYSEC-2026-161 (Host 헤더 우회 → 인증 우회) 는
# 1.0.1 에서 fix. OSV: https://osv.dev/vulnerability/PYSEC-2026-161 (CVE-2026-48710 alias)
starlette>=1.0.1,<1.1
